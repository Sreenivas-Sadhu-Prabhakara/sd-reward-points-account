package com.bank.bian.rewardpointsaccount;

import io.swagger.v3.oas.models.OpenAPI;
import io.swagger.v3.oas.models.info.Info;
import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;
import org.springframework.context.annotation.Bean;

/**
 * BIAN Service Domain: Reward Points Account
 *
 * Business Area:      Operations and Execution
 * Business Domain:    Account Management
 * Functional Pattern: Fulfill
 * Asset Type:         Reward Points Facility
 * Control Record:     Reward Points Facility Fulfillment Arrangement
 *
 * Generated by the bian-platform service generator (Phase 1 — shallow build).
 * Replace the in-memory store with real persistence and domain logic in Phase 2.
 */
@SpringBootApplication
public class Application {

    public static void main(String[] args) {
        SpringApplication.run(Application.class, args);
    }

    @Bean
    public OpenAPI openApi() {
        return new OpenAPI().info(new Info()
                .title("Reward Points Account — BIAN Semantic API")
                .description("Service Domain 'Reward Points Account' (Fulfill pattern) "
                        + "operating on the 'Reward Points Facility Fulfillment Arrangement' control record.")
                .version("0.1.0"));
    }
}
